{{--
    No facts table here: every figure behind these recommendations already has
    its own page. The mandatory closing point is not rendered in this partial,
    because partials run before the model's prose - it is appended after it, in
    pdf/report.blade.php.
--}}
